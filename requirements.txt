# Databricks App runtime dependencies.
#
# Deliberately free of any package with a compiled C extension (psycopg2,
# sentence-transformers/torch, etc). Those reliably crash the Python kernel
# with a SIGABRT on Databricks serverless compute -- including Databricks
# Free Edition, which is serverless-only. Learned on the weather-app
# homework; applied here from the start.
databricks-sdk>=0.30.0
flask>=3.0.3
pg8000>=1.31.2
requests>=2.32.3
beautifulsoup4>=4.12.3
python-dotenv>=1.0.1
